language: ruby
rvm:
  - 2.1.1
  - 2.0.0
  - 1.9.3
  - 1.9.2
  # - rbx-2
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y npm
install:
  - bundle install
  - bundle exec rake npm
